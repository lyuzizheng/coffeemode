# Fixture: the shape a bare `wrangler deploy` (or an `--env` name that is not
# declared) resolves to — top-level local-dev bindings, no `[env.*]` blocks.
# Used by tests/deploy-guard.test.ts.
name = "poi-service"
main = "../../src/index.ts"
compatibility_date = "2024-01-01"

[[kv_namespaces]]
binding = "POI_KV"
id = "22222222-2222-2222-2222-222222222222"

[[d1_databases]]
binding = "POI_DB"
database_name = "poi-store"
database_id = "11111111-1111-1111-1111-111111111111"
