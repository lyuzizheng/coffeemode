---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- The active project is a Next.js 16 full-stack app in `web/` (Postgres + Supabase auth + Cloudflare Workers).
- Legacy Vite frontend and Java backend are archived under `_archive-coffeemode-frontend/` and `_archive-coffeemode-backend/`.
