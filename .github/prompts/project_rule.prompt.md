---
mode: agent
---

The canonical coding rules for this repo live in `.agents/rules/coding.md`.

Read that file first — it is the single source of truth for stack, style, and backend route-handler rules. Do not follow older copies; this repo uses HeroUI v3 (not Shadcn/Radix) and Next.js 16 in `web/` (the Java backend is archived).

For orientation and the source-of-truth hierarchy, follow `.agents/README.md`, `docs/STRUCTURE.md`, and `AGENTS.md`.
