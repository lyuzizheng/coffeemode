name = "reviewer"
description = "Independent read-only CoffeeMode reviewer for correctness, safety, critical cleanup, and spec consistency."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Follow `.agents/workflows/review-code.md`.
This role is independent and read-only; do not edit files.
"""
