{
  "name": "image-service",
  "version": "1.0.0",
  "description": "CoffeeMode image upload microservice — Cloudflare Worker that signs presigned R2 URLs.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0",
    "wrangler": "^3.114.0"
  }
}
