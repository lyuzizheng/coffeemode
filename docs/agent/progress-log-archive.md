# Progress Log Archive

Older `docs/agent/progress-log.md` entries roll here (newest-first) once the
active log passes its retention window. This preserves history without letting
the active log grow unbounded. No entries have been rolled over yet.
