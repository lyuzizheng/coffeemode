# CafeMood Staging Stack — Dokploy Docker Compose
# Architecture: docs/specs/0005-dokploy-vps-and-deployment-architecture.md
#
# Isolated Staging Stack (BRAWUKA-241; decision 34a / BRAWUKA-240 D1b):
#   - web-staging: Next.js standalone container on internal port 3000
#   - Database: Supabase STAGING project (Postgres + PostGIS, region ap-southeast-1)
#     reached over DATABASE_URL (pooled, sslmode=require). No self-hosted
#     postgres service exists in this stack. Staging project is separate from prod.
#   - Ingress: Web container connects to external traefik-net for Traefik routing
#
# Deployment Mode Note:
#   - Plain Docker Compose (Dokploy default): deploys via `docker compose up -d`.
#   - Docker Swarm Mode: if the VPS initializes Docker Swarm (`docker swarm init`),
#     Dokploy deploys via `docker stack deploy`, which activates `deploy.update_config`.
#
# Manual Execution:
#   docker compose --env-file .env.staging -f docker-compose.staging.yml up -d
#
# Note on Compose Variable Interpolation:
#   Expressions like ${DATABASE_URL:?...} interpolate from the shell environment
#   and project environment, not the service `env_file`. When running manually
#   outside Dokploy, pass `--env-file .env.staging` so Docker Compose resolves
#   interpolations before starting containers.
services:
  web-staging:
    image: coffeemode-web-staging:${IMAGE_TAG:-latest}
    build:
      context: ../../web
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${IMAGE_TAG:-latest}
        NEXT_PUBLIC_R2_PUBLIC_URL: ${NEXT_PUBLIC_R2_PUBLIC_URL:?NEXT_PUBLIC_R2_PUBLIC_URL must be set in shell environment, via docker compose --env-file .env.staging, or Dokploy environment}
        APP_ENV: ${APP_ENV:?APP_ENV must be set in shell environment, via docker compose --env-file .env.staging, or Dokploy environment}
    container_name: coffeemode-web-staging
    restart: unless-stopped
    env_file:
      - .env.staging
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must be set in shell environment, via docker compose --env-file .env.staging, or Dokploy environment}
      # OpenTelemetry → Grafana Cloud Tempo (BRAWUKA-606). Non-secret, so it is
      # pinned here instead of left to a Dokploy env paste. The credential
      # (`OTEL_EXPORTER_OTLP_HEADERS`) stays in .env.staging.
      # `deployment.environment` matches APP_ENV — the repo's environment
      # discriminator — and is the attribute Grafana Application Observability
      # filters on.
      # No sampler on purpose (BRAWUKA-605 §6 decision 3, superseded 2026-09-21). Head sampling drops
      # whole traces at the root span, and `traces_spanmetrics_*` is derived
      # from the spans that actually arrive — so any ratio below 1 would make
      # every RED count a fraction of reality and quietly break the alerting
      # that reads them. Volume is orders of magnitude below the 50 GB free
      # tier, so 100% is both correct and cheap.
      OTEL_EXPORTER_OTLP_ENDPOINT: https://otlp-gateway-prod-ap-southeast-1.grafana.net/otlp
      OTEL_RESOURCE_ATTRIBUTES: service.name=coffeemode-web,deployment.environment=staging
    networks:
      - coffeemode-staging-network
      - traefik-net
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://127.0.0.1:3000/api/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 15s
    labels:
      - "traefik.enable=true"
      # Route staging traffic
      - "traefik.http.routers.coffeemode-staging.rule=Host(`${STAGING_DOMAIN:-staging.cafemood.app}`)"
      - "traefik.http.routers.coffeemode-staging.entrypoints=websecure"
      - "traefik.http.routers.coffeemode-staging.tls.certresolver=letsencrypt"
      - "traefik.http.routers.coffeemode-staging.service=coffeemode-staging-svc"
      # Service & Healthcheck
      - "traefik.http.services.coffeemode-staging-svc.loadbalancer.server.port=3000"
      - "traefik.http.services.coffeemode-staging-svc.loadbalancer.healthcheck.path=/api/health"
      - "traefik.http.services.coffeemode-staging-svc.loadbalancer.healthcheck.interval=10s"
      - "traefik.http.services.coffeemode-staging-svc.loadbalancer.healthcheck.timeout=3s"
      # Security Middlewares (no-op sslRedirect removed from websecure entrypoint)
      - "traefik.http.routers.coffeemode-staging.middlewares=staging-sec-headers@docker"
      - "traefik.http.middlewares.staging-sec-headers.headers.browserXssFilter=true"
      - "traefik.http.middlewares.staging-sec-headers.headers.contentTypeNosniff=true"
    deploy:
      resources:
        limits:
          cpus: "1.00"
          memory: 1024M
        reservations:
          cpus: "0.25"
          memory: 256M
  cloudflared-staging:
    image: cloudflare/cloudflared:2026.9.1
    container_name: coffeemode-cloudflared-staging
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARED_TUNNEL_TOKEN_STAGING:?CLOUDFLARED_TUNNEL_TOKEN_STAGING must be set in Dokploy environment}
    networks:
      - coffeemode-staging-network
      - traefik-net
    depends_on:
      web-staging:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 64M

networks:
  coffeemode-staging-network:
    name: coffeemode-staging-network
    driver: bridge
  traefik-net:
    external: true

