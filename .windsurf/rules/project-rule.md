---
trigger: always_on
---

The canonical coding conventions for this repo live in `docs/agent/coding-conventions.md`.

Read that file first — it is the single source of truth for stack, style, and backend route-handler rules. This repo uses HeroUI v3 (not Shadcn/Radix) and Next.js 16 in `web/` (the Java backend is archived).

For orientation and the source-of-truth hierarchy, follow `docs/agent/reading-order.md`, `docs/STRUCTURE.md`, and `AGENTS.md`.
