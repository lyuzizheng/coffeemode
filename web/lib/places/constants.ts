/** Default search radius in kilometres for POI / cafe searches. */
export const DEFAULT_SEARCH_RADIUS_KM = 10;

/** Maximum allowed nearby search radius in kilometres. */
export const MAX_SEARCH_RADIUS_KM = 10;
