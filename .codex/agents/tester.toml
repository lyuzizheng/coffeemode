name = "tester"
description = "Independent CoffeeMode tester for web flows, API routes, and user-visible behavior verification."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"
developer_instructions = """
Follow `.agents/workflows/testing.md`.
Do not edit production code; create source tests only when explicitly assigned.
"""
