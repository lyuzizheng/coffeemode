/** Shared site / PWA metadata. Keep in sync with the design tokens. */
export const APP_NAME = "CoffeeMode";

export const APP_DESCRIPTION =
  "Real wifi, outlet, and seat intel for digital nomads — find the perfect cafe to work from.";

// Derived from the --accent token (oklch 54% 0.15 42 in light theme) — keep in
// sync with app/globals.css. PWA chrome (theme-color, manifest) shows the brand
// voice, not a near-miss orange (review 2026-08-09 D5).
export const THEME_COLOR = "#b34917";

export const BACKGROUND_COLOR = "#faf8f5";
