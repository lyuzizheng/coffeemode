{
  "name": "tiles-service",
  "version": "1.0.0",
  "description": "CafeMood self-hosted basemap \u2014 Cloudflare Worker + R2. Redirects tile/style/asset requests to the per-env maptiles bucket; never proxies tile bytes.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/deploy.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy:check": "node scripts/deploy.mjs --env staging --check && node scripts/deploy.mjs --env production --check"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.119.0"
  }
}
