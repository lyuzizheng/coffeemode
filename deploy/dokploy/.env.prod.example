# CoffeeMode Production Environment Configuration Template
# Architecture: docs/specs/0005-dokploy-vps-and-deployment-architecture.md
#
# Copy to Dokploy Production Application Environment Variables (or .env.prod).
# Secrets must NEVER be committed to version control.

# Canonical public URL of Production (Apex domain, no trailing slash)
NEXT_PUBLIC_SITE_URL=https://coffeemode.app

# Allowed hosts for production redirects
NEXT_PUBLIC_ALLOWED_HOSTS=coffeemode.app,www.coffeemode.app

# Supabase Auth (Production Project)
# Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-prod-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-prod-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-prod-service-role-key

# Self-hosted PostgreSQL + PostGIS (Production Container inside Dokploy bridge network)
# Shape: postgresql://<user>:<password>@postgres-prod:5432/<db>?sslmode=disable
DATABASE_URL=postgresql://coffeemode_prod_user:CHANGE_ME_PRODUCTION_STRONG_PASSWORD@postgres-prod:5432/coffeemode_prod?sslmode=disable

# Rate limiter backend ("memory" for single app container, "postgres" for horizontal multi-instance)
RATE_LIMIT_BACKEND=memory

# POI Cache Service (Production Cloudflare Worker)
POI_SERVICE_URL=https://poi-service.your-subdomain.workers.dev
POI_SERVICE_TOKEN=prod-poi-strong-token

# Image Upload Service (Production Cloudflare Worker)
IMAGE_SERVICE_URL=https://image-service.your-subdomain.workers.dev
IMAGE_SERVICE_TOKEN=prod-image-strong-token

# Public R2 CDN host for production images
# Invariant: Must equal R2_PUBLIC_HOST ("images.coffeemode.app") in web/lib/images/constants.ts
NEXT_PUBLIC_R2_PUBLIC_URL=https://images.coffeemode.app

# Apple MapKit JS (Production Developer Account)
APPLE_MAPKIT_TEAM_ID=your-apple-team-id
APPLE_MAPKIT_KEY_ID=your-mapkit-key-id
APPLE_MAPKIT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# Observability (Optional Better Stack 429 Alert Ingest)
# BETTER_STACK_INGEST_URL=https://in.logs.betterstack.com/your-prod-token
